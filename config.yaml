hostname: "0.0.0.0"
port: 8080

servers:
  trello:
    command: bunx
    args:
      - -y
      - "@delorenj/mcp-server-trello"
    env:
      TRELLO_API_KEY: "${TRELLO_API_KEY}"
      TRELLO_TOKEN: "${TRELLO_TOKEN}"

  # Add Shopify and other MCPs here later
  # shopify:
  #   command: npx
  #   args:
  #     - -y
  #     - "@shopify/mcp-server"
